version: "2"
run:
  tests: true
linters:
  enable:
    - staticcheck
  settings:
    staticcheck:
        checks: ["all", "-ST1000", "-ST1003", "-ST1016", "-ST1020", "-ST1021", "-ST1022", "-ST1005"]
formatters:
  enable:
    - gofmt